name = "mkv-player"
compatibility_date = "2024-09-23"
pages_build_output_dir = "./dist"

# Storage bindings are not used — media and library metadata live in GCS.
# Required Pages env vars (set in dashboard):
#   GCS_BUCKET
#   GCS_SERVICE_ACCOUNT_JSON
