# @mkv/r2uploader

R2 アップロード専用 CLI です。

## 設計方針

- R2 には HLS 実体ファイルのみを保存する
- アプリ用 metadata（タイトル、タグ、説明、再生時間など）は D1 に保存する
- R2 オブジェクトメタデータはアプリの正データとして利用しない
- 最終的に、アップロード時に D1 metadata を同時登録する構成を目標とする

## 現状

- HLS ファイルを R2 へアップロードする機能のみ実装済み
- D1 metadata 同時登録は今後の実装対象
